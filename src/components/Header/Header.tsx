import React from 'react'

interface Props {
    
}

const Header = (props: Props) => {
    return (
        <div className="header">
            <h2 style={{paddingLeft: '20px'}}>TODO APP</h2>
        </div>
    )
}

export default Header;